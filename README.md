# exceltest

Next.js su tüketimi panosu: `Veri.xlsx` (Sayfa1) + `data/Nufus.xlsx` ile `npm run data` sonrası `npm run dev`.

```bash
npm install
npm run data   # Excel → data/dashboard.json
npm run dev    # http://localhost:3000
```

Kaynak: [Next.js](https://nextjs.org) (`create-next-app`).
